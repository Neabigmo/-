# R18 decision

**B — Abel bridge certified; the full Fock-determined `D(r)` relation and
coercivity remain open.**

Certified:

- the exact `Exp(1)` Abel transform for `h=b-m^2`;
- the finite positive quadratic kernel `K_ij(r)`;
- the endpoint concentration interpretation;
- an exact nonzero finite-mode witness showing endpoint non-coercivity.

Not certified:

- an all-orders formula for `D(r)` from the Fock identity;
- `H(r)<0` for every nonzero odd sequence;
- `c_n=0` or Gaussian rigidity.

The result narrows the next theory step: supply a second Fock-controlled
positive kernel or an all-orders relation for `D(r)` that controls modes whose
boundary jets vanish.
