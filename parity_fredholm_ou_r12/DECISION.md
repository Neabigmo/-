# R12 decision

`PARITY_SYMBOL_CERTIFIED_EXACT_DEFECT_MAP_REMAINS`

The exact parity identities, odd principal symbol, common symmetric zero
criterion, simple/double Hermite jet compensation, normalized `X/Y` domains,
and OU dilation covariance all pass symbolic replay.  Away from common
symmetric zeros, odd directions fill the principal jet cokernel of the even
principal block.

The exact map
`Delta_rho:Y_rho -> coker(L_R^ee)` remains unresolved because compact
perturbations deform the cokernel.  Therefore OU coherence has not been shown
to contradict a negative normalized-even index, and no Gaussian rigidity
claim is made.  No remote computation or next-stage campaign should start
from this package alone.

