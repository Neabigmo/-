# R19 — proposed all-orders source-Hessian identity audit

This branch performs the single falsifiable task requested by ChatGPT after
R18: test

\[
D(r)-\frac13=\frac12\frac{d}{dr}\bigl[rC(r)^2\bigr].
\]

The audit computes the first new coefficient, `d4`, exactly in the
characteristic-zero quotient generated by the already available `Q`-law
moment identities. It does not run optimization, a Gram campaign, an
order-8 search, or remote computation.

The result is deliberately conservative: the proposed identity fails in this
known quotient by `(2/3)c2^2`. This is not a probability-law counterexample;
it proves that the identity cannot be obtained from the currently available
finite Fock/Q relations unless an additional all-orders relation first forces
`c2=0`.

Run:

```text
python laguerre_source_hessian_r19/replay_r19.py
python laguerre_source_hessian_r19/audit_r19.py
```

Expected markers:

```text
R19_D4_MINUS_STAR_OVER_C2_SQUARE 2/3
R19_PROPOSED_SOURCE_HESSIAN_IDENTITY REFUTED_IN_KNOWN_Q_MOMENT_QUOTIENT
R19_ADDITIONAL_ALL_ORDERS_CONSTRAINT_REQUIRED
R19_AUDIT_COMPLETED
```
