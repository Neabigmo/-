# Decision

`NORMALIZATION_REPAIRED_BUT_SYMBOL_LIMIT_NOT_UNIFORM`

The ordinary Wiener scale is the correct single-radius setting for a possible
multiplier/range theorem and the radius-loss tail mechanism is explicit. The
`b`-normalized recurrence is not itself a literal Toeplitz multiplier due to
factorial shifts. Since the exact Stage-7 kernel and a uniform one-high band
limit are unavailable in this workspace, R9 stops at repaired normalization
and the precise symbol-limit gap.

