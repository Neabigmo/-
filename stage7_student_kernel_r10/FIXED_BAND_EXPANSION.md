# Fixed-band expansion

For every fixed pair `(j,k)`, the exact Student/Beta expression has limit
`(-1/2)^(j+k)`, with an explicit first `1/n` coefficient in the replay.  The
limit is taken at fixed band; the subsequent `m`-tail step is kept separate.

