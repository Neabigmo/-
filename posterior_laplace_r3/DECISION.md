# Decision

Outcome: `EXACT_CRITICAL_KERNEL_COUNTEREXAMPLE`.

The full posterior Laplace identity, conditional Q moments, formal moment
relaxation, and semigroup bridge were certified.  The first natural
Gaussian-equality critical-kernel lower bound fails on one exact two-point
heat-smoothed prior, so no parameter campaign or degree escalation is
authorized.  The remaining gap is a different rigidity mechanism for the
exact transform, or a return to the angular/entire route.
