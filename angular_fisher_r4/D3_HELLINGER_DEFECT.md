# D3 Hellinger defect

`g_z=sqrt(w_z)` 是 `2*pi/3` 周期函数。圆周 Poincaré 的第一非零频率为 3：

`H <= (1/9) average |g'_z|^2 = z^2 E_z[C_z^2]/36`。

若 `g_z=sum_n g_n exp(3 i n theta)`，Poincaré slack为

`sum_{|n|>=2} (n^2-1)|g_n|^2`，对应角谐波 6、9、12……；第一 D3 模式 3 不贡献 slack。

