# Optional symmetrization / Parseval

对 `Y=(X-X')/sqrt(2)`，有 `Psi_Y(r,theta)=|Psi_X(r/sqrt(2),theta)|^2`。若 `H_X=exp(r^2/2) Psi_X` 且角平均为 1，则 Parseval 给出

`average |H_X-1|^2=sum_{k != 0}|h_k|^2`。

首个 odd cumulant 控制第 `+/-3` 角模式的低阶项。此文件只作诊断，不把 Parseval 自动升级成刚性结论。

