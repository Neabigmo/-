# R16 decision

Decision: **D — `FORMULA_CORRECTION_REQUIRED`**.

The exact replay succeeds after two corrections:

1. the joint tilted triple law uses `exp(-qQ/(2d))`, not a positive exponent;
2. the projected sixth-order statement is `>=`, not `=`.

The corrected identities are useful and the low-order cone is insufficient, but no Gaussian-rigidity conclusion is claimed. No remote computation or new numerical campaign is authorized by this audit.
