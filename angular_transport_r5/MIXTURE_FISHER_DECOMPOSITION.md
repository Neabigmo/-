# Missing-information identity

The tilted component score is `s_theta(x)+z`, while the posterior mean of
`s_theta+x` is zero. Hence
`E_w J_theta(z)-1 = int phi(x-z) H(x) dx`.
The Gaussian kernel is strictly positive, so equality at one tilt forces
`H=0`; R5 does not assume that equality.
