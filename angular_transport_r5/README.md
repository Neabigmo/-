# Angular transport R5

This bounded branch audits the missing-information structure left open by R4.
It proves the two-dimensional tilted-mixture formulation, the rotation
continuity equation in weak form, and the exact posterior-angle Fisher identity.
The permitted product/Fisher closure is tested once and remains insufficient.

Run `F:\anaconda3\python.exe run_angular_transport.py` from this directory.
No remote computation, scan, optimizer, SDP, resultant, or high truncation is
used. The authorized next route is Stage8 critical Hellinger/parity continuum
normal form with Stage19 backward-heat positivity.
