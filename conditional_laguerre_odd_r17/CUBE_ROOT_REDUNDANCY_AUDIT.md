# Cube-root / OU redundancy audit

The cube-root triple-overlap expression arising from the R16 Gaussian-observation completion is
the same negatively tilted triple law after normalization and the corresponding variance
rescaling. It is therefore an OU-closure re-expression unless a separate argument extracts a
new cross-`x` constraint. R17 does not count it as a new rigidity mechanism.

