# R17 decision

**B — LAGUERRE_ODD_BRIDGE_CERTIFIED_INFINITE_POSITIVITY_GAP_REMAINS**

Certified in this branch:

- the `Exp(1)` Laguerre/Parseval convention;
- the tilted-mean coefficients `c1`–`c4`;
- the target moment eliminations through `mu8`;
- the conditional second-moment coefficients `d0`–`d3` and `d2=3c1^2/2`, `d3=4c1c2`;
- the tested general triangular coefficient;
- the conditional-variance realizability formulation;
- the R16 Fisher dictionary and the cube-root redundancy audit.

Not certified:

- that pointwise conditional variance positivity forces every `c_n` to vanish;
- Gaussian rigidity;
- an all-orders tail theorem.

The single remaining theorem gap is:

> Prove an infinite Laguerre conditional-variance positivity theorem for the Fock-determined
> coefficient pair `(c_n,d_n)`, strong enough to force `c_n=0`.

No R18 or computation campaign is authorized by this branch.

